// Generated by IcedCoffeeScript 1.7.1-b
describe('trakio/automagic', function() {
  var automagic, automagic_initialized, automagic_options, callback, event, form, second_form, _;
  _ = null;
  automagic = memoize().as(function() {
    return new Trak.Automagic();
  });
  automagic_initialized = memoize().as(function() {
    return automagic().initialize(automagic_options());
  });
  automagic_options = memoize().as(function() {
    return {};
  });
  form = memoize().as_haml("%form.my_form\n  %input");
  second_form = memoize().as_haml("%form.a_form\n  %input");
  event = memoize().as(function() {
    return new MockEvent('submit', form(), {
      callback: callback(),
      automagic_ready: {}
    });
  });
  callback = memoize().as(function() {
    return sinon.spy();
  });
  before(function(done) {
    return requirejs(['trakio/lodash'], function(lodash) {
      _ = lodash;
      return done();
    });
  });
  describe('#initialize', function() {
    it("initializes Automagic.Identify", function() {
      return automagic_initialized().identify.should.be.an["instanceof"](Trak.Automagic.Identify);
    });
    it("initializes Automagic.Track", function() {
      return automagic_initialized().track.should.be.an["instanceof"](Trak.Automagic.Track);
    });
    it("calls #page_ready if trak.io.page_ready_event_fired is true", function() {
      trak.io.page_ready_event_fired = true;
      sinon.stub(automagic(), 'page_ready');
      automagic().initialize();
      return automagic().page_ready.should.have.been.called;
    });
    return it("dosen't call #page_ready if trak.io.page_ready_event_fired is false", function() {
      trak.io.page_ready_event_fired = false;
      sinon.stub(automagic(), 'page_ready');
      automagic().initialize();
      return automagic().page_ready.should.not.have.been.called;
    });
  });
  describe('#page_ready', function() {
    it("adds data-trakio-automagic attribute to body", function() {
      automagic_initialized().page_ready();
      return document.body.hasAttribute('data-trakio-automagic').should.eql(true);
    });
    it("calls #bind_events", function() {
      sinon.stub(automagic(), 'bind_events');
      automagic_initialized().page_ready();
      return automagic().bind_events.should.have.been.called;
    });
    context("when bind events is false", function() {
      value(automagic_options).equals({
        bind_events: false
      });
      return it("doesn't call #bind_events", function() {
        sinon.stub(automagic(), 'bind_events');
        automagic_initialized().page_ready();
        return automagic().bind_events.should.not.have.been.called;
      });
    });
    it("calls trakio/automagic/identify#page_ready", function() {
      sinon.stub(automagic_initialized().identify, 'page_ready');
      automagic().page_ready();
      return automagic().identify.page_ready.should.have.been.called;
    });
    return it("calls trakio/automagic/track#page_ready", function() {
      sinon.stub(automagic_initialized().track, 'page_ready');
      automagic().page_ready();
      return automagic().track.page_ready.should.have.been.called;
    });
  });
  describe('#bind_events', function() {
    it("binds to all forms' submit", function() {
      sinon.stub(automagic(), 'bind_event');
      form();
      second_form();
      automagic_initialized().bind_events();
      automagic().bind_event.should.have.been.calledWith(form());
      return automagic().bind_event.should.have.been.calledWith(second_form());
    });
    return context("when there is a form that doesn't match", function() {
      value(automagic_options).equals(function() {
        return {
          selector: '.my_form'
        };
      });
      return it("only binds to matching forms", function() {
        sinon.stub(automagic(), 'bind_event');
        form();
        second_form();
        automagic_initialized().bind_events();
        automagic().bind_event.should.have.been.calledWith(form());
        return automagic().bind_event.should.not.have.been.calledWith(second_form());
      });
    });
  });
  describe('#bind_event', function() {
    return it('adds a callback to the form', function() {
      sinon.stub(form(), 'addEventListener');
      automagic().bind_event(form(), 'submit');
      return form().addEventListener.should.have.been.calledWith('submit', automagic().event_fired);
    });
  });
  return describe('#event_fired', function() {
    it("calls trakio/automagic/identify#event_fired", function() {
      sinon.stub(automagic_initialized().identify, 'event_fired').returns(false);
      sinon.stub(automagic_initialized().track, 'event_fired').returns(false);
      automagic().event_fired(event(), function() {});
      automagic().identify.event_fired.should.have.been.calledWith(form(), event(), sinon.match.func, sinon.match.object);
      return automagic().identify.event_fired.restore();
    });
    it("calls trakio/automagic/track#event_fired", function() {
      sinon.stub(automagic_initialized().track, 'event_fired').returns(false);
      automagic().event_fired(event(), function() {});
      automagic().track.event_fired.should.have.been.calledWith(form(), event(), sinon.match.func, sinon.match.object);
      return automagic().track.event_fired.restore();
    });
    return it("calls the callback once", function() {
      sinon.stub(trak.io, 'track');
      automagic_initialized().event_fired(event(), callback());
      trak.io.track["yield"]();
      callback().should.have.been.calledOnce;
      return trak.io.track.restore();
    });
  });
});
